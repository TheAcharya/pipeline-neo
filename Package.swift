// swift-tools-version: 5.4

import PackageDescription

let package = Package(
    name: "Pipeline",
    products: [
        .library(
            name: "Pipeline",
            targets: ["Pipeline"]),
    ],
    targets: [
        .target(
            name: "Pipeline",
            dependencies: []),
    ]
)
